extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 My Portfolio

      p.lead.text-light
        | This site uses Node.js, Express and Pug to showcase a few of my techdegree projects featuring: <br> * pagination, <br> * forms, <br> * game logic, and <br> * API requests
      h7 Project Links ↓
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

       each project in projects
         .cell
           a(href=`/projects/${project.id}`)
             img.thumbnail.proj(src=`/static/images/${project.image_urls[0]}`)
             h7 #{project.project_name}
